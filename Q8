<script>
/*
Develop a program to read 6 numbers and find the average of all odd numbers.

*/

//Testing: 3,6,21,2,2,2
avg_odd=0;
sum=0;

for (i=1; i<=6; i++)
{	
	num=parseInt(prompt(`${i}. Please enter number `));
    
	if(num%2==1)
    {
    	 avg_odd+= num;
         sum++;
         answer=avg_odd/sum
    }

}
alert(`The average of all odd numbers equals ${answer}`);

</script>
