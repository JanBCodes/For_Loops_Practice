<script>
/*
Develop a program that will prompt the user to enter  6 numbers and then,
print the number of positive numbers and the sum of negative numbers in the set.

*/

// Testing : 1, 1, 0, -5, -5, -5

positive_number=0;
sum=0;
zero=0;

for (i=1; i<=6; i++)
{	
	num=parseInt(prompt(`${i}. Please enter number `));
    
	if(num>0)
    {
    	 positive_number++;
    }
	else if(num<0)
    {
    	    sum+= num;
    }
    else
    {
    	zero++
     }
}
alert(`The number of positive numbers equals ${positive_number}`)
alert(`The sum of negative numbers equals ${sum}`);
alert(`The number zero is neither positive or negative. It appeared ${zero} times`)

</script>
